# Ansible setup playbooks

Ansible setup playbooks to be used with an Ansible setup inventory built with the [Ansible setup inventory template](https://github.com/debovema/ansible_setup_inventory_template).